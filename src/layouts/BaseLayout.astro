---
import "../styles/global.css";
import { assetHref, withBase } from "../lib/content";

const {
  title = "Knowledge Blog",
  description = "学习笔记、论文阅读与项目复盘的个人知识库。",
  image = "/assets/knowledge-hero.png"
} = Astro.props;

const fullTitle = title === "Knowledge Blog" ? title : `${title} | Knowledge Blog`;
const navItems = [
  { href: withBase("notes/"), label: "笔记" },
  { href: withBase("papers/"), label: "论文" },
  { href: withBase("projects/"), label: "项目" },
  { href: withBase("tags/"), label: "标签" },
  { href: withBase("search/"), label: "搜索" }
];

const pathname = Astro.url.pathname;
const homeHref = withBase("");
const pageImage = assetHref(image);
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={pageImage} />
  </head>
  <body>
    <div class="site-shell">
      <header class="site-header">
        <div class="site-header__inner">
          <a class="brand" href={homeHref} aria-label="Knowledge Blog 首页">
            <span class="brand__mark">K</span>
            <span>Knowledge Blog</span>
          </a>
          <nav class="site-nav" aria-label="主导航">
            {
              navItems.map((item) => (
                <a
                  href={item.href}
                  aria-current={pathname.startsWith(item.href) ? "page" : undefined}
                >
                  {item.label}
                </a>
              ))
            }
          </nav>
        </div>
      </header>
      <main class="page-main" data-pagefind-body>
        <slot />
      </main>
      <footer class="site-footer">
        <div class="site-footer__inner container">
          <span>Knowledge Blog</span>
          <span>Notes, papers, projects, and the trail between them.</span>
        </div>
      </footer>
    </div>
  </body>
</html>
